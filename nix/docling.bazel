"""Exposes Docling CLI binaries from the Nix-provisioned toolchain."""

package(default_visibility = ["@@//:__subpackages__"])

filegroup(
    name = "docling",
    srcs = ["bin/docling"],
)

filegroup(
    name = "docling-tools",
    srcs = ["bin/docling-tools"],
)
