--- a/src/parse/config.h	2026-04-01 10:07:09.000000000 +0200
+++ b/src/parse/config.h	2026-04-03 15:56:44.266578136 +0200
@@ -55,64 +55,64 @@
   {
     nlohmann::json j;
 
-    j["page_boundary"] = page_boundary;
+    j["page_boundary"] = nlohmann::json(page_boundary);
 
-    j["do_sanitization"] = do_sanitization;
+    j["do_sanitization"] = nlohmann::json(do_sanitization);
 
-    j["keep_char_cells"] = keep_char_cells;
-    j["keep_shapes"] = keep_shapes;
-    j["keep_bitmaps"] = keep_bitmaps;
+    j["keep_char_cells"] = nlohmann::json(keep_char_cells);
+    j["keep_shapes"] = nlohmann::json(keep_shapes);
+    j["keep_bitmaps"] = nlohmann::json(keep_bitmaps);
 
-    j["max_num_lines"] = max_num_lines;
-    j["max_num_bitmaps"] = max_num_bitmaps;
+    j["max_num_lines"] = nlohmann::json(max_num_lines);
+    j["max_num_bitmaps"] = nlohmann::json(max_num_bitmaps);
 
-    j["create_word_cells"] = create_word_cells;
-    j["create_line_cells"] = create_line_cells;
-    j["enforce_same_font"] = enforce_same_font;
+    j["create_word_cells"] = nlohmann::json(create_word_cells);
+    j["create_line_cells"] = nlohmann::json(create_line_cells);
+    j["enforce_same_font"] = nlohmann::json(enforce_same_font);
 
-    j["horizontal_cell_tolerance"] = horizontal_cell_tolerance;
+    j["horizontal_cell_tolerance"] = nlohmann::json(horizontal_cell_tolerance);
 
-    j["word_space_width_factor_for_merge"] = word_space_width_factor_for_merge;
+    j["word_space_width_factor_for_merge"] = nlohmann::json(word_space_width_factor_for_merge);
 
-    j["line_space_width_factor_for_merge"] = line_space_width_factor_for_merge;
-    j["line_space_width_factor_for_merge_with_space"] = line_space_width_factor_for_merge_with_space;
+    j["line_space_width_factor_for_merge"] = nlohmann::json(line_space_width_factor_for_merge);
+    j["line_space_width_factor_for_merge_with_space"] = nlohmann::json(line_space_width_factor_for_merge_with_space);
 
-    j["populate_json_objects"] = populate_json_objects;
+    j["populate_json_objects"] = nlohmann::json(populate_json_objects);
 
-    j["keep_glyphs"] = keep_glyphs;
-    j["keep_qpdf_warnings"] = keep_qpdf_warnings;
+    j["keep_glyphs"] = nlohmann::json(keep_glyphs);
+    j["keep_qpdf_warnings"] = nlohmann::json(keep_qpdf_warnings);
 
     return j;
   }
 
   void decode_config::from_json(const nlohmann::json& j)
   {
-    if(j.count("page_boundary")) { page_boundary = j["page_boundary"]; }
+    if(j.count("page_boundary")) { page_boundary = j["page_boundary"].get<std::string>(); }
 
-    if(j.count("do_sanitization")) { do_sanitization = j["do_sanitization"]; }
+    if(j.count("do_sanitization")) { do_sanitization = j["do_sanitization"].get<bool>(); }
 
-    if(j.count("keep_char_cells")) { keep_char_cells = j["keep_char_cells"]; }
-    if(j.count("keep_shapes")) { keep_shapes = j["keep_shapes"]; }
-    if(j.count("keep_bitmaps")) { keep_bitmaps = j["keep_bitmaps"]; }
+    if(j.count("keep_char_cells")) { keep_char_cells = j["keep_char_cells"].get<bool>(); }
+    if(j.count("keep_shapes")) { keep_shapes = j["keep_shapes"].get<bool>(); }
+    if(j.count("keep_bitmaps")) { keep_bitmaps = j["keep_bitmaps"].get<bool>(); }
 
-    if(j.count("max_num_lines")) { max_num_lines = j["max_num_lines"]; }
-    if(j.count("max_num_bitmaps")) { max_num_bitmaps = j["max_num_bitmaps"]; }
+    if(j.count("max_num_lines")) { max_num_lines = j["max_num_lines"].get<int>(); }
+    if(j.count("max_num_bitmaps")) { max_num_bitmaps = j["max_num_bitmaps"].get<int>(); }
 
-    if(j.count("create_word_cells")) { create_word_cells = j["create_word_cells"]; }
-    if(j.count("create_line_cells")) { create_line_cells = j["create_line_cells"]; }
-    if(j.count("enforce_same_font")) { enforce_same_font = j["enforce_same_font"]; }
+    if(j.count("create_word_cells")) { create_word_cells = j["create_word_cells"].get<bool>(); }
+    if(j.count("create_line_cells")) { create_line_cells = j["create_line_cells"].get<bool>(); }
+    if(j.count("enforce_same_font")) { enforce_same_font = j["enforce_same_font"].get<bool>(); }
 
-    if(j.count("horizontal_cell_tolerance")) { horizontal_cell_tolerance = j["horizontal_cell_tolerance"]; }
+    if(j.count("horizontal_cell_tolerance")) { horizontal_cell_tolerance = j["horizontal_cell_tolerance"].get<double>(); }
 
-    if(j.count("word_space_width_factor_for_merge")) { word_space_width_factor_for_merge = j["word_space_width_factor_for_merge"]; }
+    if(j.count("word_space_width_factor_for_merge")) { word_space_width_factor_for_merge = j["word_space_width_factor_for_merge"].get<double>(); }
 
-    if(j.count("line_space_width_factor_for_merge")) { line_space_width_factor_for_merge = j["line_space_width_factor_for_merge"]; }
-    if(j.count("line_space_width_factor_for_merge_with_space")) { line_space_width_factor_for_merge_with_space = j["line_space_width_factor_for_merge_with_space"]; }
+    if(j.count("line_space_width_factor_for_merge")) { line_space_width_factor_for_merge = j["line_space_width_factor_for_merge"].get<double>(); }
+    if(j.count("line_space_width_factor_for_merge_with_space")) { line_space_width_factor_for_merge_with_space = j["line_space_width_factor_for_merge_with_space"].get<double>(); }
 
-    if(j.count("populate_json_objects")) { populate_json_objects = j["populate_json_objects"]; }
+    if(j.count("populate_json_objects")) { populate_json_objects = j["populate_json_objects"].get<bool>(); }
 
-    if(j.count("keep_glyphs")) { keep_glyphs = j["keep_glyphs"]; }
-    if(j.count("keep_qpdf_warnings")) { keep_qpdf_warnings = j["keep_qpdf_warnings"]; }
+    if(j.count("keep_glyphs")) { keep_glyphs = j["keep_glyphs"].get<bool>(); }
+    if(j.count("keep_qpdf_warnings")) { keep_qpdf_warnings = j["keep_qpdf_warnings"].get<bool>(); }
   }
 
   bool decode_config::load(const std::string& filename)
