# Based on the upstream pull request:
# https://github.com/docling-project/docling-parse/pull/184
diff --git a/src/parse/page_item_sanitators/cells.h b/src/parse/page_item_sanitators/cells.h
index 8da1331..6f606d4 100644
--- a/src/parse/page_item_sanitators/cells.h
+++ b/src/parse/page_item_sanitators/cells.h
@@ -118,8 +118,8 @@ namespace pdflib
 
 	  item["rendering_mode"] = cell.rendering_mode;
 
-	  item["widget"] = cell.widget;
-	  item["left_to_right"] = cell.left_to_right;
+	  item["widget"] = nlohmann::json::parse(cell.widget ? "true" : "false");
+	  item["left_to_right"] = nlohmann::json::parse(cell.left_to_right ? "true" : "false");
 	}
 
 	result.push_back(item);
diff --git a/src/parse/page_items/page_cell.h b/src/parse/page_items/page_cell.h
index 0f5a4ff..84941be 100644
--- a/src/parse/page_items/page_cell.h
+++ b/src/parse/page_items/page_cell.h
@@ -209,8 +209,8 @@ namespace pdflib
       cell.push_back(font_key); // 17
       cell.push_back(font_name); // 18
 
-      cell.push_back(widget); // 19
-      cell.push_back(left_to_right); // 20
+      cell.push_back(nlohmann::json::parse(widget ? "true" : "false")); // 19
+      cell.push_back(nlohmann::json::parse(left_to_right ? "true" : "false")); // 20
 
       cell.push_back(has_graphics_state); // 21
       cell.push_back(utils::values::round(line_width)); // 22
diff --git a/src/parse/qpdf/to_json.h b/src/parse/qpdf/to_json.h
index e6864fd..d71acce 100644
--- a/src/parse/qpdf/to_json.h
+++ b/src/parse/qpdf/to_json.h
@@ -184,7 +184,7 @@ namespace pdflib
         else if(obj.isBool())
           {
             bool val = obj.getBoolValue();
-            result = val;
+            result = nlohmann::json::parse(val ? "true" : "false");
           }
         else
           {
